# The-Meal-DB-Explorer
TheMealDB Explorer is a Spring Boot web application that consumes the public TheMealDB API to search and explore meals by name, category, and area, with server-side rendering using Thymeleaf and in-memory caching for performance.
